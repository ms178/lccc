# AArch64 session 61 evidence

* `gcc-torture-500-summary.json`: post-fix first-500 GCC execute results and
  every remaining failure identity.
* `benchmark-vm-screening.md`: paired randomized fastbuild LCCC/integrated
  lccc-ld versus GCC runtime screening.

The complete raw object trees are intentionally regenerated rather than stored.
GNU assembler 2.47 is mandatory in `aarch64_execute_suite.py`.
