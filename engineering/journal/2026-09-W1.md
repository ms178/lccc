# Journal — September 2026, days 01–05 (W1)

Distilled from 57 per-session documents (deleted after consolidation,
2026-09-16). The arc: IR-verifier birth → the MachInst typed-lowering
build-out → the PR #358–#364 Tier-2 red-team cycle → the RA cost-model /
miscompile-hunting wave. The method (same-window paired A/B, red-team
before belief) crystallises in this week and is now standing policy in
[`../agent/RULES.md`](../agent/RULES.md).

Entry tags: `[fix]` live defect · `[goal]` measured win · `[neg]`
do-not-retry · `[infra]` gate or tool now load-bearing · `[rec]` method /
handoff · `[audit]` verdict on someone else's work.

---

## 09-01 — ir-verify-zero: 396/1112 → 0 violations
<!-- src: engineering/FOLLOWUP-2026-09-01-ir-verify-zero-and-first-backend-win.md -->

The 08-31 structural verifier, driven to zero across all six opt levels
(3354 configs) and made a permanent gate (`CCC_VERIFY_IR=1|abort|strict`).
The first backend-quality win measured under the clean corpus: the
`movslq` retirement line (matchlen kernel −35.3 %). Loop_unroll's
two-block carried-phi fix verified against upstream PF-17's independent
fix — theirs (`init` incoming for outside uses) is the correct polarity. [fix][infra]

## 09-01b — `range_fold` never fired; the harness is the deliverable
<!-- src: engineering/FOLLOWUP-2026-09-01b-range-fold-and-measurement-discipline.md -->

`range_check.rs` existed *for* the `x >= lo && x <= hi → (unsigned)(x-lo) <= hi-lo`
Expat/SQLite idiom and fired on none of it — its cast-peel allow-list
missed the frontend's bool-widening cast. Fixed; `k_classify`
**−18.7 %**. Two harness defects had poisoned statistics: the benchmark
harness itself had never been delivered as a gate, and it *could
manufacture regressions and wins* by ordering. Discipline recorded (now
RULES §Benchmarks): paired same-window A/B, negative-control passes,
kill-switch isolation; never compare ratios across reports. Known
follow-up created here: block layout emits 3 branches/iter where
GCC/Clang emit 2 — the seed of 01c/01j. [goal][rec]

## 09-01c — loop-aware block layout: `memchr` −40 % → parity vs GCC
<!-- src: engineering/FOLLOWUP-2026-09-01c-block-layout-and-loop-inversion.md -->

Two changes: loop-aware layout keeps loop bodies contiguous and
**machine-level loop inversion** duplicates the condition so hot loops
fall through instead of jumping back. `memchr` to GCC parity (later
named "Machine-level loop inversion" in the root backlog's closed
table). Env-knowledge preserved: a harness wipe returns
`~/.cargo/bin/rustup` without its exec bit and without the `cargo`/`rustc`
proxies — `rustup dispatch on argv[0]`; `scripts/lccc-bootstrap.sh`
`setup_rust` repairs both (symlink proxies), since
`rustup toolchain install` does **not** recreate them. [goal][infra]

## 09-01d — census-first `narrow_copy_fold`; MachInst gets its first tests
<!-- src: engineering/FOLLOWUP-2026-09-01d-copy-fold-and-machinst.md -->

Previous copy-fold measured 10 insns across 220 files and was deleted —
right call for *that* implementation, wrong conclusion about the
opportunity. Rebuild started from a final-asm census at `-O2` over the
559-test corpus: `movq` cross-family 7 943 · `movl` cross-family 2 463 ·
identity `movl %X,%X` 261 — i.e. ~12× the real opportunity. MachInst had
946 lines and zero tests; the session built the suite's first layers and
corrected two neighbouring tests (not weakened). Key escape-followup:
`iv_widen` declines whenever the IV escapes (→ 01e, 01k). [goal][infra]

## 09-01e/f — escaping-IV + vectorized-counter miscompile, fixed properly
<!-- src: engineering/FOLLOWUP-2026-09-01e-escaping-iv-and-machinst-fuzz.md, FOLLOWUP-2026-09-01f-vectorized-counter-proper-fix.md -->

01e found (via seeded fuzz) the vectorizer counter miscompile: the
element counter was re-used as the remainder IV. 01f's proper fix: the
remainder loop gets its own element counter; measured 4.5× on the
kernel with no regression elsewhere. Also from the fuzz wave: the imul-S8
finding — **no two-operand 8-bit imul exists**; widen S8→S32 before the
multiply rather than faking an 8-bit form (MachInst census rule). [fix]

## 09-01g — MachInst execution differential + classifier revert
<!-- src: engineering/FOLLOWUP-2026-09-01g-machinst-execution-differential.md -->

Third differential layer landed: 4000-insn seeded fuzz → GAS 2.47
assembly differential → **execution differential** (compile, run, hash
outputs on both compilers). The byte-classifier chain attempt (`a||b||c`
to `Select`s) was reverted the same day: without critical-edge
splitting of the shared increment-block edge it breaks SESE
if-conversion (worst kernel 0.44×). PF-CLS-1 owns the retry with the
split first. [infra][neg]

## 09-01h/i — MachInst coverage: measured, then 74.5 % → 85.1 %, then guarded
<!-- src: engineering/FOLLOWUP-2026-09-01h-machinst-coverage.md, FOLLOWUP-2026-09-01i-machinst-85-percent.md -->

Coverage was unmeasurable — on `false` the instruction silently fell
back to text emit. `CCC_ISEL_STATS=1` prints the census + ranked
rejection breakdown. Baseline 53.9 % on the spot sample; 74.5 % full
corpus. Landed: `GlobalAddr` (13.6 %), narrow stores, `ParamRef`
963 → 96 rejections (proper no-code subset; the earlier blanket-
`true` attempt broke four tests and was reverted — the rules: alloca-homed
params and stack-passed args still reject, and the fallback reads the
parameter's *incoming* register even when a caller-saved pre-store of a
different parameter aliased the name). Coverage guard: a regression
test fails if any class the layer owns drops back out. `Call` (7.8 %,
largest remaining class) deliberately **not** lowered: the rejection
boundary *is* the clobber model — `Call` before per-instruction
clobbers is a miscompile (→ 02c/02e close it properly). [goal][infra]

## 09-01j — a 19 % layout defect, and the attribution method that found it
<!-- src: engineering/FOLLOWUP-2026-09-01j-block-layout-order-preserving.md -->

Self-rebuttal kept verbatim: two listed "regressions" (arith_loop,
spectral_norm) were cross-report ratio comparisons — meaningless. The
same-window kill-switch A/B exposed the real defect on adler32 (−19 %):
**RPO is a linearization, not a layout** — linearizing by RPO order
split loop bodies arbitrarily. Fix: order-preserving layout (start from
the existing order, fix only contiguity). `CCC_NO_BLOCK_RELAYOUT` is the
kill switch (note: it disables the *whole* pass — historical). Two
hypotheses had been implemented and measured wrong first — keep the
pairing rule in RULES §Attribution. [fix][rec]

## 09-01k — `iv_widen` fires on constant scales: sieve −21.6 %
<!-- src: engineering/FOLLOWUP-2026-09-01k-iv-widen-scaling.md -->

`iv_widen` had tests, ran in the pipeline, and fired on ~none of the
loops it was written for (probe with a three-line program before
believing a pass works). Fixed to fire through constant `Shl`/`Mul`
scales (variable scales deliberately excluded). Same-window A/B with
`CCC_NO_IV_WIDEN`, every output byte-identical: sieve −21.6 %,
nbody −3.0 %. The corpus run and the A/B *disagree* on sieve — and both
are correct: only one of them is an attribution. Say which measurement
supports which claim. [goal][rec]

## 09-01l — IV closure widening: designed, measured, **reverted**
<!-- src: engineering/FOLLOWUP-2026-09-01l-iv-closure-attempt.md -->

Widening only the IV leaves `a[i-1]`, `a[(i&7)+1]`, `a[i*3]`
narrow-derived; the design admitted the whole derived closure proven
in-range by interval analysis. It miscompiled `sqlite_varint`
(`8e8824b0a241168` vs `deedcdd4edc1c0f1`) and revealed two soundness
bugs worth the knowledge:

1. **`Or` range bound is provably wrong**: `hi | c` is not an upper
   bound (`x∈[1,2], c=2` yields max 3 vs bound 2). Interval machinery
   must do bitwise range analysis for `Or` or not admit it.
2. **Trip bound read from the wrong comparison**: any `Cmp` mentioning
   the counter is not the exit condition.

Shipped instead: the missing correctness gate —
`scripts/check_benchmark_outputs.sh` compiles/runs/diffs every
`tests/benchmark/programs/*.c` at all four opt levels (~4 min, no
timing); 152/152. **A miscompile had passed all 563 regression tests
because those kernels were only ever built by the timing harness.**
Retry needs the seed-relation membership proof (record which closure
value each member derives from + `debug_assert!`); prize already
quantified: sieve −26.5 %, tls_seg −5.7 %, loop_patterns −6.5 %. [fix][infra][neg]

## 09-01 notes — ISel ROI, PF-17 wrap-up, PR #325–#328 audit
<!-- src: engineering/notes/2026-09-01-isel-roi.md, notes/2026-09-01-isel-roi-oracle.md, notes/2026-09-01-pf17-remaining-three.md, notes/2026-09-01-pr-325-328-audit.md -->

Ten peephole/ISel wins from the `isel_roi.c` oracle screen (direct
popcnt/lzcnt/tzcnt without staging; Intel false-dep `xorl` before them;
`movl` on `operand_to_{rax,rcx}` for non-negative imm32; typed
`const_as_imm32`; `cmp $0`→`test`; `is_read_modify_write` dest-only).
Documented open: variable rotate (16 vs GCC 4 — later closed as
MI-ROTATE-1), `blsr`/`blsi`/`bzhi` pattern match, 32-bit `leal`, do
**not** `cmovl` a signed I32 blindly, do **not** retarget 64-bit `leaq`
under 32-bit `movl`. PF-17 remaining-three: rotate-ON A/B 19 MATCH / 0
FAIL, rotation stays opt-in; Guards D (latch-incoming defined in
header) and E (skip nested loops); univsr skip of rotated self-loop
pointer phis; unroll header-phi `Copy(init)`; post-merge audit of PRs
#325–#328 clean. [goal][fix][audit]

## 09-02 — general-cloner revival: the cloner that could never fire
<!-- src: engineering/FOLLOWUP-2026-09-02-general-cloner-revival.md -->

Audit of the c72ff2c review: stride scaling existed but the general CFG
cloner could not fire on *any* standard loop — the `inst_ok` pointer-
origin walk failed **closed** at depth 8 spinning `phi → Add → phi`
through the IV cycle, so every loop died at the check (instrumented:
20/20 attempts). Fixed (walks the cycle, open by shape); unroll
arithmetic hardened. Standing rule for walkers: recursing over a phi
cycle must memoize, or "conservative" becomes "universal false". [fix][rec]

## 09-02 — Agent B `iv_widen` audit + three test repairs
<!-- src: engineering/FOLLOWUP-2026-09-02-iv-widen-agentb-audit.md -->

Agent B's iv-widen suite adopted with third-party test quality fixed in
place: their `iv_widen_latch_and_bound.c` `runtime_derived(a, N-1)` with
N=400 read `a[i*3]` up to `a[1194]` on a 400-element array (UB; made the
GCC differential vacuous — the same smear as Agent Z's harness later);
fixed to N=420/`N/3` (max index 417). Added `iv_widen_unsigned_runtime_
bound.c` and extended `vectorize_narrow_iv_reduction.c` (non-zero-start
int/long/dot cases). [fix][audit]

## 09-02 — session 10: kernel-boot + zstd + Tier-2 state
<!-- src: engineering/FOLLOWUP-2026-09-02-session10-kernel-boot-zstd-tier2.md -->

Kernel-boot quest checkpoint (base `2ae63f82`): Tier-2 unique-slot is
the fallback; `run_slot_stress.sh` harness landed (seeds 1..20 — the
range that would later hide seed 32 until 09-03 extended it). Open
carry: the `-O1` oracle regression (§7.1, upstream CallTyped) still
open at week end. [rec]

## 09-02b — Agent-B rebase + latch `lea_base_fold`
<!-- src: engineering/FOLLOWUP-2026-09-02b-agentb-rebase-and-lea-base-fold.md -->

Loop-latch LEA base fold peephole (`CCC_PEEPHOLE_SKIP=lea_base_fold`).
Environment contract restated for all later sessions: swap file via
`scripts/ensure_swap.sh` (both build wrappers call it), fastbuild via
`scripts/build_lccc_fast.sh` (opt-level 1, no LTO, incremental — full
build ~2 min, incremental 10–45 s on the 2-core sandbox), oracle pins
GCC 16.2=`cg162`/Clang 23.1=`cclang2310`/ICC 2021.10=`cicc2021100`/
ICX=`cicxlatest` verified, and the recorded gap at the time: **no
linker oracle existed yet** (mold/lld not installed; candidate for a
future session — closed in W3). [goal][infra]

## 09-02c — MachInst typed scalar-float layer (FMov/FAlu)
<!-- src: engineering/FOLLOWUP-2026-09-02c-machinst-float-layer.md -->

`MachReg` had no XMM representation — *every* float instruction fell to
untyped emission. Landed: `FMov` (S32→`movss`, S64→`movsd`; reg↔reg
uses the VEX 3-operand merging form `vmovsd %s,%s,%d` — mirrors the
text path's nbody false-dependence fix), `FAlu` (`v{add,sub,mul,div}s{s,d}`
VEX three-operand; Sub/Div pin operand order; commutative swap when
only rhs is xmm-homed), xmm8–15 as `PhysReg 26..=33`, strict `freg_name`
(no silent fallback — house rule). Double-gated admission (isel +
emit-side `float_typed_candidate` must agree); kill switches
`CCC_MI_DISABLE_KINDS=float-mov|float-alu`. Census 88.05 → 88.99 %
(+788 lowered; ~284 float moves were pre-stats invisible, so real gain
≈ +3.6 pp). Found via oracle: pre-existing vectorizer-orphan ICE —
`ir/intrinsics.rs` `is_pure` Vec-family fix + post-vectorize DCE.
Backlog: typed `Call` (522 rejections, 48 % of rest) — done 02e;
vectorizer global-array map bailout; dot8 (no 2-trip full unroll,
FMA gating); FCmp scratch representation. [goal]

## 09-02d — PR #353 merge repair + scalar FMA destination paths (S13–S22)
<!-- src: engineering/FOLLOWUP-2026-09-02d-pr353-repair-fma-paths.md -->

Multi-snapshot arc. (1) The #353 checked-bail hoisting was directionally
right (bail *before* IR mutation — red/green-proven) but the merged tree
did not compile (E0425 in the general cloner; duplicated test module;
shadowed old check kept). Repaired + regression test
`general_cloner_overflow_bail_leaves_cfg_untouched`. (2) dot8 FMA
funnel: 37 → 33 → 29 insns via dst-path selection (lhs xmm home as
src1; shared-home `vfmadd213sd` landing the result in the home) — first
213-form cut misfired and was caught in validation. (3) `fold_fma_
memory_src2` peephole: single-use `[v]movs{d,s}` load folded into the
adjacent FMA3-231 memory-src2 slot (function-wide last-mention liveness
proof; **call veto for xmm0-7** — callee-saved-vs-argument hazard);
10 unit tests, `CCC_PEEPHOLE_SKIP=fma_mem_fold`; runtime at GCC parity
(306–311 ms both, 100M-iter). (4) #355/#356/#357 red-teams: gcc's dot8
strategy (zero-accum `vfmadd132sd`), Godbolt-oracle FMA shaping,
SSE2 constant-load pathology (`fp_const_hoist`,
`CCC_PEEPHOLE_SKIP=fp_const_hoist`, 9 tests). (5) **GAS 2.47 oracle
pinned** (`~/.cache/gas-2.47-x86_64/bin/as`; distro 2.44 is NOT an
oracle) — i686 encoder comments re-captured and found identical; NOP-run
layout carries the 2.47 flip (`count/11 > 7`). Rebase onto #358 clean;
unit 1615/0, regression 571/0. [fix][goal][infra]

## 09-02e — `CallTyped`: SysV call as one atomic machine instruction
<!-- src: engineering/FOLLOWUP-2026-09-02e-machinst-calltyped.md -->

Closes the clobber-model condition set in 01h: `CallTyped { caller_saves,
args, target, ret }` carries declared clobbers (full SysV caller-clobber
set) and uses — the way MIR `CALL` / Cranelift `call` model it. Atomic
under replay fallback: one IR instruction ↔ one MachInst, so a mid-run
fallback re-emits through the mature path with no double-emission.
Pure builder `isel::build_typed_call` consumes pre-resolved argument
homes and returns the execution-ordered move plan — full admission/
ordering matrix pinned by tests. Also landed: xmm0/xmm1 pre-colored
float scratch + slot-homed float relays. Rationale kept: the mature
path staged every arg through the rax accumulator (two moves where one
suffices) and the dead staging survived the peephole. [goal]

## 09-02f — BitTest(base, 0) fold; MachInst floatconst + indirect calls
<!-- src: engineering/FOLLOWUP-2026-09-02f-bittest-index-zero.md, FOLLOWUP-2026-09-02f-machinst-floatconst-indirect.md -->

`BitTest` with constant index 0 folds to the canonical parity test
(merged upstream with the slot-stress harness as PR #361). MachInst
float-constant materialization and indirect-call typed staging followed
(09-03b-typed-i128-pair in the same line: typed I128 through the
rdx:rax pair classes). [fix]

## 09-03 — rebase + `flush_machinst` shadow fix
<!-- src: engineering/FOLLOWUP-2026-09-03-rebase-and-flush-shadow.md -->

`flush_machinst()` early-returned with an empty buffer but left the IR
shadow `machinst_buf_ir` populated by no-op lowerings (a coalesced
`Copy` whose src==dest home emits zero MachInsts yet is recorded). A
later flush — possibly in a *different function* — replayed stale IR:
`drivers/tty/tty_io.c` ICE (`operand_to_rax: value 12 has no register
home`). Fix clears the shadow on the no-op path. Kernel-boot acceptance
still open at week end (documented as such; recovery recipe for the
6.18.47 tree + cachymod patchset recorded — superseded by
`scripts/arena_session_restore.sh`). [fix]

## 09-03 — PR #358 audit: width veto kept, Tier-2 re-enabled
<!-- src: engineering/FOLLOWUP-2026-09-03-tier2-audit-landed.md, AUDIT-2026-09-03-PR358-tier2.md -->

Verdict: keep #358's width veto (hardened to a widest-wins fixpoint
type map) and **disagree** with disabling Tier-2 — re-enabled by
default with a closed-boundary hull (fat-interval) interference model;
per-segment sharing is A/B-only opt-in; setjmp keeps unique-slot
fallback. Machinery: `CCC_VERIFY_SLOT_WIDTHS` verifier, `CCC_DEBUG_SLOTS`
accounting, 8 unit tests incl. randomized brute-force interference
cross-checks, 3 C regressions, Tier-2 A/B differential as regression
step 3b; slot-stress made deterministic (`region_setjmp` locals
`volatile` — indeterminate-after-longjmp oracle fix; factorial uses the
real knobs). unit 1626/0 · regression 575/0 · slots 400/400. [fix][audit]

## 09-03 — FP-fold liveness gap + natural-width selects
<!-- src: engineering/FOLLOWUP-2026-09-03-pr358-359-redteam-fp-fold-gap.md, AUDIT-2026-09-03-natural-width-conditional-selects.md -->

Second independent #359 soundness hole: the liveness refinement
accepted a fold when the first later mention "fully overwrites" the
loaded xmm, but `is_pure_xmm_overwrite` classified reg-reg scalar
`movsd`/`movss`/`vmovsd`/`vmovss` as full overwrites — they are **merge
writes** (SDM: upper bits preserved), and the preserved bits can come
from the very load being deleted. Rule: only a memory-source scalar
form defines the whole register. Fixed `722f7c3c`. Same-day width work
(AUDIT-natural-width): ternary merge slot forced target-int width —
C `int` selects must stay 32-bit (`cmovl`/`movl`; widen/north Cast
pairs removed). Fixes: `ternary_arm_type` reports the C-semantic arm
type (I64-storage literal of C type int → I32); `get_expr_type` for
conditional arms matches; merge stores narrow constants to slot type;
`emit_implicit_cast` normalizes integer constants to the declared
source width. i686 unaffected by design (ILP32: target int is I32). [fix]

## 09-03 — `o2_vla_fill`: narrow store of a large immediate
<!-- src: engineering/BUG-2026-09-03-O2-vla-store-miscompile.md, AUDIT-2026-09-03-PR363-and-rebase.md, AUDIT-2026-09-03-PR363-364-direct-stores-and-zero-guards.md -->

Slot-stress extended beyond seeds 1..20 hit seed 32 at -O2/-O3 in all
four layout configurations (layout-independent = the tell). Root: `Store
{ val: Const(I64(v)), ty: U32 }` (VLA fill `b[i] = 6u*2654435761u + i`)
lowered to `Mov { Imm(v), mem, S32 }`, and the emitter's large-immediate
branch ignored `size` and always used the S64 shape. Contract fixed
(`b10ef6c0`): the `mov{b,w,l}` immediate field is a RAW {8,16,32}-bit
value — a direct sized `movl $3041712678, mem` truncates by hardware to
exactly the destination width; the relay (%rax at operand width) stays
only for S64-dest >i32. Upstream fixed the same bug independently in
#363 via the relay: ours is strictly better (8 → 4 insns, 0 %rax
clobbers; kept theirs where it is the right form). Also deduped
clz/ctz zero-guards at IR level for every backend, and an isel fix:
**never split a volatile F64/D64 const store into two movl halves**.
Evidence: slot-stress 1..45 × 5 levels = 900/900; pins:
`vla_largeconst_u32_fill.c`, `wide_const_imm_stores.c` +
`check_wide_const_imm_stores.sh` (default + `CCC_MI_ALL_CLASSIC=1`). [fix]

## 09-03 — GEP-chain folded-address liveness triad (session 30b)
<!-- src: engineering/FOLLOWUP-2026-09-03b-gep-chain-fold-liveness.md -->

Three defects, three kinds of wrong: (A) sqlite `-O1` **SIGSEGV** (open
since session 11) — `rbp=0x1` isn't a frame-pointer mystery, rbp *was*
the loop counter; chained-GEP `extend_gep_base_liveness` now classifies
hard non-fold vs address-link uses, computes foldability as a fixed
point, and walks the **whole composed chain** at every folded access,
stopping exactly where the backend materialises its own address (second
variable index, displacement outside i32). Cost recorded honestly: +0.15 %
.text on sqlite3.c. (B) `reuse_redundant_loads` folded a load whose
destination is one of its own address registers — wrong pointer from an
inlined hash lookup. (C) `reassoc_accum` applied unconditionally was a
**1.7–2× pessimization** on its own flagship pattern (+30–80 % size) —
cost model now gates it. adler8 103 → **70** insns (gcc 16.2 = 129);
adler32_do8 5.41 → 4.79 ms (gcc 4.00). gep-chain stress: 11/600
failures unfixed → 0 fixed. [fix]

## 09-03 — direct-store family + typed-i128 + unroll/vector merge audit
<!-- src: engineering/FOLLOWUP-2026-09-03b-pr364-redteam-direct-stores-clz-zero-guards.md, FOLLOWUP-2026-09-03b-typed-i128-pair.md, FOLLOWUP-2026-09-03c-unroll-vector-redteam-merge.md, AUDIT-2026-09-03-pr359-362-redteam.md, AUDIT-2026-09-03-pr363-364-fp-fold-call-veto.md, AUDIT-2026-09-03-wide-cond-zero-test.md, AUDIT-2026-09-03-PR359-fp-folds.md -->

Wrap-up of the #359–#364 cycle: #360 flush-shadow / small-slot gate /
size-aware layout — AGREE; #361 BitTest + slot-stress — AGREE; #362
fixpoint value-type map + hull coloring + Tier-2-on — AGREE with the
one correction on its claim (VLA bug present at `c8683317`, fixed by
S26 replay). fma-mem-fold keeps the **call veto for xmm0–7**. `cmpq
$0`→`testl` guidance (use the condition's IR width). i128 typed pairs
and the unroll/vectorize conflict merged with red-team. [audit][fix]

## 09-04 — PR #426 CI root cause; narrow-condition width; peephole-oracle audit
<!-- src: engineering/PR426-CI-I686-ROOT-CAUSE.md, AUDIT-2026-09-04-narrow-condition-test-width.md, AUDIT-2026-09-04-ultimate-vs-rebase.md, engineering/notes/2026-09-04-peephole-oracle-audit-447ea90e.md, engineering/notes/2026-09-04-phi-coalesce-rebase-5649e27.md -->

PR #426's i686 SIGSEGVs were **environment**: `/lib/ld-linux.so.2`
present but `/usr/lib32` missing — an incomplete multilib host links a
dynamically-linked ELF32 whose libc startup image is malformed and
SIGSEGVs *before* the C function runs. CI now installs `gcc-multilib`
+ `libc6-dev-i386` first; without them the torture runner reports 1676
`reference-compile-skip` and **passes vacuously** — check the pass
count, not just exit status (a trap that recurred all month). Agent Z's
MachInst narrow-slot promotion adopted (sound width argument:
S64 substitutions can't read a 4-byte spill slot; promote to a window
register, reload S32 = zero-extend defines full GPR); their
release-build/reduced-target CI changes rejected. Narrow-condition
repair (`%cl`/`%cx`/`%ecx` width-matched names on the classic compare
emitters incl. replay) and the "ultimate" tree A/B adjudicated; PRs
#396–#400 peephole-oracle audit green (1854 unit); 09-04 rebase note:
upstream #393 pulled in what had been lost-session work (base split-latch
phi coalescing, `%rdx` param-homing gate, CVP depth-capped look-through,
vec_load_sink use-site remap, dead_writes LEA-fold) — taken from
upstream in conflict resolution; only the surviving own layer
re-applied. [fix][audit][rec]

## 09-05 — RA cost-model camp + torture: the five-miscompile day
<!-- src: engineering/FOLLOWUP-2026-09-05-regalloc-cost-model-and-torture.md, FOLLOWUP-2026-09-05d-ra06-and-key-injectivity.md -->

Four miscompiles fixed in session 5-1 (`iv_widen` float-conversion
closure — 10 shapes; `builtin_size_t_arg_width`; nested-function static
chain in regalloc; `DivRem` pair opposite flavour; `vector_guard_sum`
pre-existing exposed). Session 5-4 found five more, all **silent
wrong-address / deleted-access** classes — the canonical address-key
soundness rules landed here:

- A cast in an address key may be descended only when provably
  value-preserving **and** sign/half-preserving: sext and zext of the
  same source must not share a key (`d[i]` vs `d[255]` with
  `i = -1`), covered-arm forwarding must not reach 256 bytes away,
  `sink_conditional_stores` must never merge stores to different
  addresses.
- The SIB-index cast peel `I32 → U32 → I64` must not walk to the I32
  root and *sign*-extend it — every char/CRC/tolower table lookup hits
  this shape (`t[(unsigned char)c]` with `c=-1` addressed `t[-1]`).
- `rewrite_covered_arm_loads` matched `Load{..}` **blind to
  `volatile`** — C11 5.1.2.3 requires both reads; match must gate.

Torture state: 8380-config runs, **0 run-fail** on both x86-64 and
i686 (remaining non-pass = host-GCC skips, plus `pr117432` needs the
C23 one-arg `va_start` we added and GCC 14.2 lacks). Diagnosis recipe
carried into RULES: `CCC_DISABLE_PASSES=<pass>` bisects to a single
pass in seconds; `CCC_DUMP_EACH_PASS=1 CCC_DUMP_FUNC=<fn>` shows its IR
delta; always confirm pre-existingness by rebuilding at base before
attributing. [fix][infra]

## 09-05 — RA-06 built, proven correct, measured negative — **shipped off**
<!-- src: engineering/FOLLOWUP-2026-09-05d-ra06-and-key-injectivity.md (§2), ../DECISIONS.md RA-06 entry -->

Pressure-driven reload-at-next-use as an IR pre-pass: correct on all
gates, **negative** on the kernels it was built for. DECISIONS.md
carries the what/why/what-would-change-it and the also-invalidated
variants (zero-future-use eviction, accumulator-leader promotion).
Standing conclusion: the spill gap is not a splitting-knob problem —
it is in-loop pressure *splitting with reloads at use*, which the IR
pre-pass cannot express. [neg]

## 09-05b — Agent Z audit + RA-28/28b resolved with data + C23 dialect gaps
<!-- src: engineering/FOLLOWUP-2026-09-05b-agentz-audit-c23-ra28.md, FOLLOWUP-2026-09-05c-fable-reconstruction-ra28b.md, AUDIT-2026-09-05-agentz-followup.md -->

Audit verdict (full table in the AUDIT doc): ADOPT four of six commits,
hardened; REJECT the two harness taxes. Adopted: address-key `Shl`
scale injectivity (a real address-aliasing miscompile), if-conversion
speculation **shape-heuristic → dereferenceability rule** (the best
change in the patch), `fp_copy_web_groups` coalescing of scalar-FP
webs, `vex_promote` BFS liveness/direct-emit. Closed residual holes the
adoption exposed: interference check failed **open** (missing segment
== "no interference"; flipped to fail-closed), speculation coverage
ignored access **extent**, truncating casts collapsed in the key.
Rejected: four inert `LCCC_NO_COMPARE=1` sidecars (validated by nobody —
their harness can't tell a sidecar took effect) and the gutted
`vectorize_map_expr_tree.flags` rewrite. Process rules recorded: a
series whose first commit is a miscompile repaired in the third is not
bisectable — adopt squashed, always-correct units. C23: `__STDC_VERSION__`
selects on `-std=` (FE-30; every header took the pre-C23 branch before)
and one-argument `va_start` (FE-31). New tool `scripts/ra_ab_census.py`:
mode-6-vs-3 measured — **default stays 3**; the *why* (RA-28b) is that
the kernels mode 6 loses are call-free leaf loops where every eviction
is a permanent lifetime demotion, and the existing call-splitter can
never fire (`CCC_SPLIT_MAX=200` moves exactly **0** instruction on the
corpus). Mode 6 is a prerequisite *for* RA-06's in-loop splitting, not a
partial implementation of it. Fable reconstruction: **CG-07** — `-0.0`
in static data collapsed to `.zero` (`IrConst::is_zero()` is C
truthiness; `-0.0 == 0.0` is true; measured `1/t[0] → +inf` want `-inf`)
fixed with bit-exact zero detection; the fourth strict min/max fold
(VEC-11) and integer map lane ops (VEC-10) landed with hash-identical
differentials. [fix][audit][neg]

## 09-05e — OP-42 / CG-09 / measured normal-`-O2` inlining repair
<!-- src: engineering/FOLLOWUP-2026-09-05e-op42-cg09-inline-policy.md -->

Landed: atomic expression-chain sinking only when the transaction is
semantics-safe *and* complete-external-input-cost Pareto-profitable
(89adb5c3; supersedes the 09-0x "atomic chain sinking REJECTED"
entry — the fix was the length-aware model); longest-common-**suffix**
epilogue sharing with SysV callee-save-tail narrowing + CFI/order
hardening (−109 insns corpus); normal-`-O2` inline policy repairs
(plain static multi-site wrapper hiding an inlineable loop descendant
stays outlined — `spectral_norm` 0.821×; loop helper outlined when its
call site is in an enclosing loop — `hash_table` 0.841×; ordinary small
loop helper clone cap 4 sites — glibc derived workload 0.8942); TLS
CSE class merge (foldable+must-materialize unified — 3→2 address
sequences; direct `%fs:symbol@TPOFF` lowering is the next step). [goal]

## 09-05 — vec_interleave: reduction accumulator interleaving
<!-- src: engineering/notes/2026-09-05-vec-interleave-40593cea.md -->

New IR pass splitting canonical 2-block vector reductions into
interleaved accumulator chains. Dot F64: **0.16 ns/elem vs GCC 0.30**
L1-resident (1.9× faster; 7.6× vs lccc's single chain); L2/L3 at GCC
parity (2.6–3.5× vs the kill-switch arm). Knobs:
`CCC_NO_VEC_INTERLEAVE=1`, factor override `CCC_VEC_INTERLEAVE=2|4|8`,
`CCC_DISABLE_PASSES=vec_interleave`. Open: relax the max/multi-acc
rejection (seed-reuse correctness proven, needs a workload);
`vhaddpd` combine tree; the two `movslq` IV sign-extensions; AArch64
NEON needs emitter-disp support before un-gating. [goal]

## W1 close-out — i686 ALU + FP min/max (docs consolidation note)
<!-- src: docs/I686_ALU_AUDIT.md, docs/FOLLOWUP_I686_ALU.md, docs/FOLLOWUP_FP_MINMAX_SELECT.md -->

The two living docs pairs triaged here for durability: the i686 ALU
audit (base `82d7009`) established the `synth_mul` exhaustive bounded-
search multiply synthesiser and the 3122-function differential red-team
(`scripts/i686_alu_redteam.py`); its open items at week end were F-1
leaf prologue/homing (the largest gap), F-2 `andl $31` redundancy, F-3
rotate recognition — the whole i686 arc continues in W3 and is
consolidated in [`../subsystems/i686.md`](../subsystems/i686.md). The FP
min/max/compare/blendv line (Fable #2, S06 through S11-ULTIMATE) proved
every fold **lane-exact** against real `MINPS`/`MAXPS` semantics with
full NaN/±0 differentials, survived two outside revisions (R1/R2/R4
audit repairs; vex_promote refinement), and its explicit process lesson
— *confident prose about "every soundness claim backed by an executable
regression" was falsified by three uncovered holes* — is why RULES
§Differential now requires mutation-verified tests. Durable remainders
filed under OP-05/VecOpInfo. [rec]

---

## Deleted-source map (57 documents → entries above)

`engineering/FOLLOWUP-2026-09-01{,b,c,d,e,f,g,h,i,j,k,l}-*.md` (12) →
one entry each · `engineering/FOLLOWUP-2026-09-02{,-general-cloner-revival,-iv-widen-agentb-audit,-session10-kernel-boot-zstd-tier2,b-agentb-rebase-and-lea-base-fold,c-machinst-float-layer,d-pr353-repair-fma-paths,e-machinst-calltyped,f-bittest-index-zero,f-machinst-floatconst-indirect}.md` (9) → one entry each ·
`engineering/FOLLOWUP-2026-09-03{,-pr358-359-redteam-fp-fold-gap,-rebase-and-flush-shadow,-tier2-audit-landed,b-gep-chain-fold-liveness,b-pr364-redteam-direct-stores-clz-zero-guards,b-typed-i128-pair,c-unroll-vector-redteam-merge}.md` (8) ·
`engineering/FOLLOWUP-2026-09-05{,-regalloc-cost-model-and-torture,b-agentz-audit-c23-ra28,c-fable-reconstruction-ra28b,d-ra06-and-key-injectivity,e-op42-cg09-inline-policy}.md` (5) ·
`engineering/AUDIT-2026-09-03-{PR358-tier2,PR359-fp-folds,PR363-364-direct-stores-and-zero-guards,PR363-and-rebase,natural-width-conditional-selects,pr359-362-redteam,pr363-364-fp-fold-call-veto,wide-cond-zero-test}.md` (8) ·
`engineering/AUDIT-2026-09-04-{narrow-condition-test-width,ultimate-vs-rebase}.md` (2) ·
`engineering/AUDIT-2026-09-05-agentz-followup.md` ·
`engineering/BUG-2026-09-03-O2-vla-store-miscompile.md` ·
`engineering/PR426-CI-I686-ROOT-CAUSE.md` ·
`engineering/notes/2026-09-01-{isel-roi,isel-roi-oracle,pf17-remaining-three,pr-325-328-audit}.md`,
`engineering/notes/2026-09-04-{peephole-oracle-audit-447ea90e,phi-coalesce-rebase-5649e27}.md`,
`engineering/notes/2026-09-05-vec-interleave-40593cea.md` (7) ·
`docs/{I686_ALU_AUDIT,FOLLOWUP_I686_ALU,FOLLOWUP_FP_MINMAX_SELECT}.md` (3,
durable content moved to `subsystems/i686.md` / `subsystems/optimizer.md`).

All entry bodies carry measured numbers only where the source showed a
same-window A/B or an oracle count; everything else is stated as
triage. Full validation tables live at the upstream merge commits each
entry names.
