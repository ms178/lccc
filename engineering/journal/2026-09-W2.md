# Journal — September 2026, days 06–12 (W2)

Distilled from 47 per-session documents (deleted after consolidation,
2026-09-16). The arc: Rust-modernisation landing → the biggest peephole
/ allocator week of the project (rotates, `GlobalAddr` CSE, FP
minmax, byte-lane maps) → the loop-alignment + identical-blocks
cycle → GLA Phase 1 / web-wide in-loop-use supply → the first i686
audit wave. PR #410-era merges are pinned individually per entry.

Entry tags per [`README.md`](README.md): `[fix] [goal] [neg] [infra]
[rec] [audit]`.

---

## 09-06 — Rust 1.80→1.98.1 / Edition 2024 + 39-kernel corpus + oracle screen
<!-- src: engineering/FOLLOWUP-2026-09-06-RUST-MODERNIZATION-AND-WORKLOAD-CORPUS.md, FOLLOWUP-2026-09-06B-REDTEAM-AUDIT-PATCH-VERDICT.md -->

Modernisation (all upstream as PR #430 `a459716`): `LazyLock` over
manual `OnceLock` boilers, `is_none_or`/`is_some_and`/`is_ok_and`
idioms, `split_at_checked`, `div_ceil`/`align_up`, Edition 2024 with a
warning-free contract. Corpus grew 33 → 39 deterministic kernels:
`chacha20_block` (ARX + rotate + 4-way parallelism), `sha256_transform`
(ROR + schedule expansion), `linux_rbtree` (parent_color tagging),
`zstd_count` (ctz + unaligned loads), `lz4_compress` (match loops),
`glibc_strstr` (Two-Way) — provenance recorded in
`tests/benchmark/WORKLOAD_PROVENANCE.md`. The B-suffix red-team
documented that the "perfected" patch was already 100 % upstream
(byte-identical tree check — an audit pattern worth reusing). Recorded
gaps for later: ChaCha quarter-round vectorization (SDOT-style ARX lane
task), LZ4 store-forwarding, PF-17, CRC-table SIB addressing. [infra]

## 09-06 — PR #425 CI green + PERF-41 audit + CI-hygiene series
<!-- src: engineering/FOLLOWUP-2026-09-06-pr425-ci-green.md, FOLLOWUP-2026-09-06-pr425-perf41-audit.md, FOLLOWUP-2026-09-06-rust2024-ci-hygiene.md -->

Three root causes for the Rust-2024 CI red: rustfmt drift after the
edition migration; a table-driven regalloc test asserting the wrong
default (`RaConfig` parser defined `CCC_MI_MAX_LOOP_INSTS` default
elsewhere); and CI building one profile while gating another (the job
now uses `scripts/build_lccc_fast.sh` and consumes its artifact).
Lessons recorded verbatim: a differential test must not print raw
topology/identity state — print invariants (#431's green was luck, its
APIC-print was a scheduling-dependent flake); *a passing step proves
nothing about determinism*; audit for multi-core scheduling variance
with affinity pinning. PERF-41 (multi-site loop-clone pressure) audited
and carried with its structural CI oracle. [fix][infra]

## 09-06 — kernel-boot C1–C5: memcpy staging + prologue ABI reads
<!-- src: engineering/FOLLOWUP-2026-09-06-kernel-boot-c1-c5-prologue-memcpy.md -->

C1/`mc2`: `emit_memcpy` staged `dest→%rdi` and `src→%rsi` **each through
`%rcx`** while parameters still lived in ABI registers — the first
move destroyed the second argument. Fixed with ordered parallel-copy
staging (`f(struct S *src, struct S *dst)`: `src∈%rdi, dst∈%rsi`
under `x86_param_caller_homes_safe`). Plus the C5 prologue fix (ABI
register reads before any param home clobber). Gates: 2019/0 unit,
646/0 regression. [fix]

## 09-06 — MachInst window register allocator
<!-- src: engineering/FOLLOWUP-2026-09-06-machinst-window-allocator.md -->

`machinst_alloc.rs` (~700 lines): MachInst was a tested pattern library
with a **replay cliff** — one spilled value in a register-only position
(two-address ALU/`Lea`/`SetCC` dest, memory base/index) flushed the whole
buffered window through the accumulator text path. The window allocator
keeps spilled values in real GPRs from the shared identity (no new
location class — integration contract with the main RA through
`resolve_stack_vregs`). Structural gates pin representative windows
against `MI-FALLBACK` regressions. [goal]

## 09-06 — MS-08: invocation-owned `RaConfig`
<!-- src: engineering/FOLLOWUP-2026-09-06-ms08-ra-config.md -->

`RaConfig` is now the **sole parser and owner** of the audited RA policy
surface: one immutable `Arc<RaConfig>` captured per
`compile_to_assembly`, threaded through passes, `CodegenOptions/State`,
all four backend constructors, stack-layout/RA, MachInst selection and
the phase-4 peephole gate. 73 distinct literal inputs (56 presence
switches, 15 textual, 3 numeric); `CCC_NO_*` names kept as literal
parser inputs (compatibility policy); `LCCC_DUMP_IR` deliberate stays
driver-level. Scope boundary (v3 audit): stack-layout axes
(`CCC_NO_SMALL_SLOTS`, `CCC_NO_TIER2_GRAPH`, `CCC_NO_SLOT_COALESCE`,
copy_coalescing's knob) intentionally keep per-invocation env reads —
they are A/B axes by regression-suite design. Why it's
behaviour-preserving: presence switches `var_os().is_some()`, text
`var().ok()`, numeric fallbacks/clamps unchanged; the
`run_inline_phase` `CCC_DUMP_IR` condition is threaded explicitly (the
one plumbing defect `cargo check` caught). Tests inject synthetic
env-sources instead of mutating process state. [infra]

## 09-06 — PF-15 byte-carrier narrowing, measured asymmetric
<!-- src: engineering/FOLLOWUP-2026-09-06-pf15-widened-byte-carriers.md -->

Landed with guardrails (now BACKLOG PF-15): single-use widen used only
as a branch condition → narrow source; two single-use identical
**signed** widens feeding a signed/equality cmp → compare the narrow
sources. The unsigned cross-block pair form was semantically *valid*
but made real Expat reproducibly slower (a lower static count did not
outweigh RA/layout perturbation) — keep it adjacent-only until placement
costs are modelled. Tooling repair worth naming: `bench_kernels.py`
claimed interleaving but ran constant order; now reverses/rotates per
round, supports a named alt-env arm, pins children, records the
environment — un-mislabelling the earlier pair-only numbers. [goal][rec]

## 09-06 — PF-17 header-effects + narrow-compare width
<!-- src: engineering/FOLLOWUP-2026-09-06-pf17-header-effects-narrow-cmp.md -->

Two rotation blockers fixed (rotation stays opt-in): Guard F —
`loop_rotate` refuses any header non-`Phi` instruction outside the SSA
closure cloned to the latch (arbitrary header work has observable
order; `for (i = 0; (trace[i] = 7+i*13), i < n; ++i)` proves the need);
classic compare emitters name the width-matched `%rcx` sub-register
(`%cl`/`%cx`/`%ecx`) after staging a narrow operand, in both normal and
replay emission. Full opt-in corpus: 666 pass / 0 fail. Follow-up
recorded: any future header-work support must model ordered replay, not
weaken Guard F. [fix]

## 09-07 — kernel-boot C6: `-Os` aggregate-frame veto + the payload bisection
<!-- src: engineering/FOLLOWUP-2026-09-07-kernel-boot-c6-os-agg-frame.md -->

C2 SIMD/ISA gate re-verified (vectorizer + FMA fold honour TU flags; 0
xmm/ymm/zmm in all 1011 LCCC-compiled kernel TUs; the 11 `.S`
crypto objects carry ~5055 SIMD refs legitimately). New: the `-Os` cost
model counted callee instructions but never the callee's **stack
frame** — a static callee whose only cost is a large non-promotable
alloca was inlined per call site (`getchar_timeout` shipped 3× frame).
`inline.rs` now carries `agg_frame_bytes` and vetoes. `boot_stage_
bisect.sh` refines the 2×2 with Kbuild `.cmd`-record flag parity (43
flags each, set difference empty): **lccc setup + gcc payload BOOTS;
lccc + lccc SILENT → the real-mode/setup stage is exonerated; the
defect is in the compressed payload.** A wrong claim made earlier the
same day was publicly retracted and replaced with this evidence (the
harness note about `make` not rebuilding when the compiler *binary*
changes — delete the `.o`; `objdump -d` on `-m16` objects decodes as
32-bit, use `objcopy -O binary` + `objdump -D -b binary -m i8086`).
Real-mode corpus still 1.80× GCC with `mov` +1138 (~55 % of delta)
materialisation traffic. Boot corpus 24320 vs GCC 13479 B. [fix][rec]

## 09-07 — loop-rotate Guard G: the recorded rationale was wrong
<!-- src: engineering/FOLLOWUP-2026-09-07-loop-rotate-guard-g.md -->

`STATE.md` listed "eliminate the extra entry jump" as the PF-17
rationale; `loop_rotate.rs` itself recorded v16 default-negative. This
session rebuilt the measurement chain honestly: rotation's
profitability is workload-shaped (memory-bound loops pay ~nothing,
branch-tight loops pay ~1 branch/iter). Guard G = profitability gate,
not correctness. **Upstream later landed the full rotation work (PRs
#439 loop-rotate pressure gate + #440 native rotates).** Applies the
same rule as PF-15: read the pass's own recorded measured answer
before re-measuring it. [rec]

## 09-07 — native rotates + imm32 hoisting + `GlobalAddr` dup-merge
<!-- src: engineering/FOLLOWUP-2026-09-07-rotate-imm32-gaddr-then-slot-traffic.md -->

(A) Native `RotateLeft/Right` (variable amounts, all four backends,
MachInst path): chacha20 9.86× → **4.41×** vs GCC, sha256 2.76× →
2.04×; also fixes a latent MachInst window-regalloc `unreachable!()`.
(B) imm32 **bit-pattern** constant hoisting (lz4 +1 % [0.984,0.994];
removes a per-iteration load for every 0x9E3779B1-class hash
multiplier) — and un-dead-locks the AArch64 arm (the pass was a no-op
for its motivating case there — probe before believing a pass works).
(C) Duplicate variable-index `GlobalAddr` merge: expat **−23.3 %**,
lz4 **−15.3 %**, geomean 0.943/7 benchmarked. Its initial
miscompile `pic_indexed_store_static_global.c` was **not in C**: the x86
peephole's `line_writes_memory` found the destination with
`rfind(',')`, which for `movb %al, (%r10,%r12)` lands on the SIB comma —
indexed stores were invisible to the guard stopping
`reuse_redundant_loads` from reusing a pre-store load. Fixed at the
source (`last_top_level_comma`), whole class audited, 6 tests. C's
merge merely made the address indexed and *exposed* the hole — the
class of bug increased optimizer reach keeps finding (see 09-09
memcpy). [goal][fix]

## 09-07 — ARX/ChaCha series + RA-PRESSURE-1 admission cap
<!-- src: engineering/FOLLOWUP-2026-09-07-ARX-CHACHA-RA.md -->

Work-v4 on `ca1c3b34` (PRs #439/#440 merged): verifier
def-dominates-use checks + cloned-remainder base dominance; InlineAsm
outputs modelled in the verifier; alias-guard bases soundened; loop_unroll
exit-phi entry operand; **constant-fold + copy-prop BEFORE the
aggregate SROA split** — the split only models GEPs whose offset
*operand* is a literal, so folding first is what turns unrolled
offsets into literals: chacha20 10.014× → **2.85×** vs GCC. Bit-idioms
soundness: idiom identity must not cross integer casts (three live
miscompiles — `bit_idiom_soundness.c` locks it). RA-PRESSURE-1
loop-span admission cap implemented (`CCC_RA_LOOP_SPAN_RESERVE`,
default 0 = off) with the full census record (decision/premise
superseded by the 09-11 valve diagnosis — the boolean-only supply is
what shipped). PR #440 hard truth recorded: it had been merged
**without a single test run** of the rotate PR (#440 was sound after
audit, but the process note stands). [goal][fix]

## 09-07 — v5: position-aware `%rdx` admission, PF-07 demotion, RISC-V immediates
<!-- src: engineering/FOLLOWUP-2026-09-07-v5-rdx-hazard-pf07-riscv-imm12.md -->

(A) **Position-aware `%rdx` admission** (RA Phase 2-x64): one cold
`a % b` used to evict `%rdx` from a whole function — lz4's `UDiv`
evicted it from the match-search loop. Position-aware hazard
partitioning re-homes 11+ values, frame −32 B
(`CCC_NO_RDX_HAZARD` restores whole-function exclusion). (B) PF-07
post-RA demotion: promoted indexed-symbol bases the RA could not home
no longer emit the dead `leaq sym(%rip),%rax; movq %rax,slot` pair —
consumers take the per-access remat they always took. (C) RISC-V
immediate model for `int_const_hoist`: the *driver gate* had silenced
the pass (same failure shape as AArch64's); constants outside imm12 now
hoist (4 multi-insn `li` removed per motivating iteration), imm12
single-`addi` correctly stays. (D) i686 operand-split canonicalization.
Script consolidation wave P1/P3/T2/R2 executed simultaneously:
`fuzz_diff.py` engine registry (9 engines absorbed), `perf_ab.py`
preset dedup, `census_full_delta.sh` pair mode, `--skip PASS`. Carried
backlog: hot-loop alignment (±3–5 % alignment lottery on lz4 alone),
RA stack-traffic wall (spill_weight still under-ranks long-lived
loop-carried values; 6-callee-saved budget binds lz4-class functions:
49–51 frame movs vs GCC 10), RISC-V `addi`-form folds, i686
mirror of the x86 phi-coalesce hazard hole (pre-existing). [goal]

## 09-08 — OP-05c..g byte-lane maps + OP-06 IV fusion (the big vectorizer day)
<!-- src: engineering/FOLLOWUP-2026-09-08-OP05CD-BYTE-LANE-MAPS.md -->

The gzip/expat/curl/SQLite parser family (where GCC/Clang/ICX win with
`vpcmpeqb`-class shapes), 12 numbered moves: unsigned range-mask fusion
for all integer lane widths (x86 has no packed unsigned compare below
AVX-512 — the sign-bias remap + `vpcmpgt` was 5 insns/predicate and the
`lo<=c&&c<=hi` window paid it twice; fused to `vpaddb`-biased forms);
all-homed three-operand VEX fast paths; **i8/u8 conditional map
vectorization**; select strength reduction (packed-only); short-circuit
if-combine loop-body scoped + single-bit window union (`isalpha`
idiom); runtime byte invariants + signed byte min/max; the
`fold_int_minmax` non-confluence fix; OP-06 induction-variable fusion
(the element counter retires: byte offset == counter×32 → `cmp` the
offset). Two CI failures along the way are the durable lessons: (1) the
memfold-aware resolver round — *making all 53 `reg_assignments` lookups
memfold-aware is an unbounded proof obligation; the audited-consumer
gate is the shape to ship*; (2) `MapEmitCtx` vs `emit_map_scalar_tree`
are **two interpreters of one `MapExpr`** — three of five defects in
the series were signedness/replication/element-size disagreements
between them, each right for the first `n − n%VF` elements and wrong
for the tail: the `map_expr_interpreter_tests` now assert semantic
agreement on 3000 random trees/lane width + the load-bearing invariant
(masks appear only in condition positions: packed compare gives
all-ones, scalar gives 0/1). 16-bit lanes correctly re-use the byte
machinery (`narrow_domains(n)`), with `pmullw` admitted at 16-bit and
refused at 8, signed word min/max SSE2-baseline vs byte's SSE4.1.
Tooling: `scripts/hot_loop_metric.py` — static whole-function counts
*reward scalar refusal*; the metric now is steady-state density
(insns in loop body ÷ bytes consumed per trip), loop selection by
maximum bytes-per-trip. Open: 2× unroll of the packed map body (the
only remaining Clang gap; 4× there). Structural debt named for the
next consolidation: five overlapping op-classification tables (three in
`regalloc.rs`, two in `copy_coalescing.rs`) need a single `VecOpInfo`
table. [goal][fix][rec]

## 09-08 — loop alignment + `.p2align` GAS parity + the `identical_blocks` miscompile
<!-- src: engineering/FOLLOWUP-2026-09-08-loop-alignment-identical-blocks.md, AUDIT-2026-09-08-PR444-redteam.md, AUDIT-2026-09-08-S03-supreme-merge.md -->

Three lands: (1) full GAS `.p2align N,fill,max-skip` grammar with
relaxation-safe markers; every semantic verified against a locally
built **GNU as 2.47** (`scripts/ensure_gas_247.sh`) before writing
code — including the corners nobody probes: `.p2align 4,,10`
declining to pad still raises `sh_addralign`; `.p2align 63` at offset 0
silently does nothing; empty fill field selects default fill. 776/776
asmdiff. (2) Dedicated hot-loop alignment pass with GCC-16.2-derived
directive policy + `-falign-*` flags (`CCC_NO_LOOP_ALIGN=1` kill;
audited by `check_loop_alignment.py` reconstructing the CFG). (3) The
alignment pass deterministically exposed `identical_blocks` eating
a vectorized zeroing loop (`vmovdqu %ymm0,(%rdi)` with **no setup
between guard and first store** — pointer/counter/accumulator never
initialized; the block scan stopped at non-op directives). Fixed with
the 4-piece soundness set (directive transparency, entry-region
pseudo-predecessors, fall-through-entry deletion guard,
merged-duplicates keep trailing directives). Second same-day finding:
partial unroll of a side-effecting-latch loop miscompiled at -O3
(20/20; the unroller cloned body work but never the latch)
→ rejection + `outer_loop_shapes.c` corpus. The same-day audit table
(PR #444 vs my #445) ended: #444 adopted wholesale as the better S01 —
*they verified against the oracle before writing code; I was self-
consistent but oracle-untested at the corners*; the PR #445/#446
archaeology (never-merged branches, judged file-by-file merges) lives
in AUDIT-S03-supreme-merge. F1 there: the abs-fold subtrahend fix
(`v<0 ? 0-(v+1) : v` misfolded to `max(v,-v)` — silent wrong answer;
`is_neg_of` guard) survived because no CI suite compiled that shape at
vectorization flags — `run_correctness.py` is now gated in
`ci_local.sh`. Validation matrix: 655 regression / 776 asmdiff /
180 bench outputs / A/B geomean 0.9949 (alignment pays on real-silicon
DSB; policy is GCC's hardware-validated one). [goal][fix][audit]

## 09-08 — unified 442+445 + ChaCha diagnosis
<!-- src: engineering/FOLLOWUP-2026-09-08-UNIFIED-442-445.md -->

PR #442 was committed broken (14 compile errors: remainder-planning
`depth`/`done` threading and the as-committed outer call would have
reset the depth cap + diamond memo *silently disabling the cycle
guard*; clippy `redundant_field_names`; bench record) — three root
causes fixed and validated. `__attribute__((cold))` wired (parser flag →
`FunctionAttributes` → section + skip); trip-4/5 boundary behaviours
pinned. ChaCha20 absolute diagnosis (post-#448): scalar 415 ms vs ICX
237 ms — the 16-word state in stack slots, every ARX operand a folded
memory operand; GCC/Clang auto-vectorize the quarter-round at -O2 v3
(execution A/B proving the admission-cap arms at allowed=0). The
**ARX lane-vectorization** design (column pack + `vpshufb`
diagonalisation + `vpaddd/vpxor`/shift-or-lowered rotates; vprold under
EVEX) is the competitive answer — recorded as the next epic's
spec seed. `CCC_NO_SPAN_VALVE` diagnostic switch added. [fix][rec]

## 09-09 — constant-size `__builtin_memcpy` → native `Memcpy`, and what it exposed
<!-- src: engineering/FOLLOWUP-2026-09-09-memcpy-lowering-and-fp-soundness.md -->

`read64`-style `T v; __builtin_memcpy(&v,p,8); return v;` lowered to a
libc call, leaving a store-to-temp+reload in every unaligned hot load.
Now lowered to `Instruction::Memcpy`; SROA forwards the load.
**Both latent soundness holes it exposed are the lesson**: (1)
`fold_ptr_deref_through_stack` folded `movq (%ptr),%rax; movq
%rax,slot; movsd slot,%xmm` → `movsd (%ptr),%xmm` without bailing on an
intervening store with no GP dest (`movq $0,(%rsi)`, aliasing at
runtime) — `fp_liveness_ptr_deref_alias_negative` returned `3 1`, now
`3 3`; (2) `aggregate_sroa` load-forwarding treated two distinct pointer
values as non-aliasing (returned 1 vs 3 at -O2/-O3) — the source must
be a private non-escaping alloca or the target one, otherwise assume
aliasing. Regression: `memcpy_unaligned_load_fwd.c`. Oracle standing
after: zstd_count 1.34× → 1.22×. Cross-backend oracle + four
miscompiles companion doc (root) carried the ARM/RISC-V
copy-coalescing ports and the restore recipe that became
`scripts/arena_session_restore.sh` (`one command: swap, toolchain, apt
deps, exec bits; cargo build --profile fastbuild ≈ 2m30s`). [fix][goal]

## 09-11 — rot16 ARX `-O2` miscompile: RA was legal, the peephole was wrong
<!-- src: artifacts/repros/rot16-arx-O2-miscompile.md -->

`out[5]` low by exactly 1024 (bit 10) at -O2/-O3/-Os in rotate/ARX
chains — root cause is `fold_accumulator_alu_store`
(`local_patterns.rs`): the RA assignment was **legal**; the peephole
rewrote a `pure`-class write whose source register the folded store's
RMW read. Lesson pinned for the whole class: *dump pre-peephole asm
first* (`LCCC_NO_PEEPHOLE=1` makes the IR-vs-asm check one line) and
never trust a killswitch sweep alone — the earlier harness exported
`CCC_CCC_NO_X` variables no code reads (the sweep that lied);
`artifacts/repros/killswitch_bisect.sh` + `delta_shrink.py` are the
kept tools, `tests/regression/peephole_acc_fold_arx_src_kill.c` the
gate, falsifiability proven by short-circuiting `pure_family_write`.
The `aggregate_sroa` split merely removed frame slots and pushed hot
scalar code onto the buggy path — the second case where *increasing
optimizer reach* was the reliable miscompile-finder. [fix][rec]

## 09-11 — phi-acyclic copy order: the answer depends on the base
<!-- src: engineering/FOLLOWUP-2026-09-11-phi-acyclic-copy-order.md, AUDIT-2026-09-11-S12-redteam.md, rebase evidence dirs -->

The document self-supersedes, and the supersession is the durable
content: resolver runtime cost was **not** a loop-carried range
lengthened across the back edge, and RA-06 was **not** the
prerequisite. Post-rebase amplified factorial on `25ed36de`: web-wide
in-loop-use supply alone = **+3.63 %/+4.33 %** sha256 (median and min
agreeing, p=0.0000) → **ships**; phi-resolver on top = **−1.99 %/−0.71 %**
→ **withdrawn from default** (stays opt-in `CCC_PHI_ACYCLIC_ORDER=1`).
But on `4f527199` the un-amplified ~55 ms/arm median said the *exact
opposite* (+8.21 %, `median_and_min_agree` violated, min ratio +3.8 %).
RULES §30 now encodes it: **re-derive every factorial conclusion after
a rebase; never let an un-amplified median choose what ships; amplify
to ≥ 200 ms/arm.** The blast-radius screen that caught the follow-on
(`scripts/differential_corpus.sh`, 807 TUs byte differential) found a
**40.53 % lz4 regression** the aim-only measurement missed — time only
what names TUs after a static-corpus screen, never only the kernel you
aimed at. The S12 self-audit also records the stale-comment fix and
the property-5 gate (`lz4_compress` byte-identical supply on/off,
mutation-verified). [goal][neg][rec]

## 09-11 — vectorizer S09/S10 miscompiles + loop-idiom v1.1 roadmap
<!-- src: backlog.md narrative sections (folded 2026-09-16) -->

**S09 cond-store use-without-def (pre-existing, fixed).** PR #492 CI red
blamed the S07 if-combine guard; `CCC_NO_IFCOMBINE=1` panics on the
pristine base too — a **latent vectorizer bug the guard exposed by
declining**. Root: `rewrite_conditional_store` empties every non-first
chain block, but `emit_cond_mask_selects` re-emitted non-first compares
with their *original* operands — the `Cast` died in the cleared second
condition block. Fix: an operand-availability gate (outside values pass
through; inside values get their pure def chain re-materialized —
never div/rem; everything else declines, scalar stays byte-exact).
`k_inplace_three` now vectorizes fully (32-wide AVX2) instead of
crashing. **S10 IV-live-out (pre-existing, fixed).** `wsum` got the
vector iteration count (15) instead of the trip count (511): the map
and stencil transforms redefine the scalar counter per vector iteration
but never called `rewire_escaping_iv_uses` — the reduction paths
already did. Fix generalizes the rewire (any IV + pass tag, also
`Store.ptr`), retargets exit-phi loop-side incomings at the remainder
header, and gates both analyzers with `loop_escape_closed`. Zero-cost:
loops stay vectorized. **S08-loop-idiom v1.1** is an opt-in pass whose
measured reach is near zero (lz4 hot loops are single-block /
pointer-IV / param-rooted); the extension order is single-block loops →
pointer-IV (`Ult` on pointer phis, trip = `end − start`) → preheader
splitting → `__restrict__` param roots (unlocks libc-style copies); the
match-copy smear case must **never** match. Flip default-on only after
corpus A/B + fuzz on the final guard set. Live items are in
[`../../backlog.md`](../../backlog.md) PF-LZ4-1 / S08-next. [fix][rec]

## 09-11 — GLA Phase 1 + source-less remat → default ON
<!-- src: engineering/FOLLOWUP-2026-09-11-global-location-allocation-phase1.md, ../DECISIONS.md RA-GLA-01/02/03 -->

Global Location Allocation P0-A Phase 1 (the doc proposed a new
`src/backend/location_alloc.rs` — not present at base `8ca2fd4`;
gate `CCC_RA_GLOBAL_LOCATION=1` default OFF; `split_ranges` lazy
entry-alloca + planning helpers). Decision record on file: RA-06's
intra-block Belady-MIN proof — partial intra-block spill-then-color is
a dead end for loop-carried φ (the full why is in DECISIONS RA-GLA-01,
plus the environment/fail-closed-knob contract). Source-less remat
graduated default-ON the next day (RA-GLA-02) with the fire census,
four-target runtime equivalence, edge-trampoline coverage, and the
second-red-team hardening; per-target reach bands set with evidence
(aarch64 6→10, riscv stays 6 — RA-GLA-03). [goal]

## 09-11 — valve cost-blindness: proposed remedy **falsified — do not retry**
<!-- src: engineering/FOLLOWUP-2026-09-11-valve-cost-blindness.md -->

Supplying merged coalesce members' in-loop reads into the admission
cap's use *counts* (not just the boolean) costs **40.53 % on
lz4_compress** (min ratio 1.4078, p=0.0000) — mechanism via
`CCC_TRACE_ALLOC=1`: the count-coupled veto strands cheap spans and
hands pressure to a cost-blind valve that spilled the wrong victim
(v212 remcost 100 evicted → v133 remcost 1110 spilled). Ships instead:
boolean-only supply + the property gates. "OPEN, root-caused" is now a
closed direction. [neg]

## 09-11 — slot-load dedup: implemented, sound, **not shipped (nil payoff)**
<!-- src: engineering/FOLLOWUP-2026-09-11-x86-slot-load-dedup.md -->

sha256 oracle standing (the table worth keeping): clang 126 insns/0
frame refs · gcc 154/1 · **lccc 198/52 refs over 14 distinct slots** —
hottest `360(%rsp)` ×10 (the `u32 *state` param, stored once, reloaded
per use in the epilogue). The load→load-forwarding peephole was built,
proven sound (paired A/B, kill switch), and **does not pay**: the ARM
`eliminate_repeated_slot_loads` analogue caches `(frame byte,is_word)→reg`
but the loop has no free register to hold the cached value — that *is*
the problem. Corrected target: P0-C (the epilogue's reload-CSE shape is
a location-model gap, see RA-31/GLA). Adjacent trap recorded so the
reasoning is not re-derived: `has_indirect_mem → invalidate_all` is
over-conservative for read-only indirect operands, but refining it must
exclude `xchg/xadd/cmpxchg/lock/rep/string ops` — and the payoff is nil.
[neg][rec]

## 09-12 — S15/S16/S17 red-team arc + census-zero
<!-- src: engineering/AUDIT-2026-09-12-S15-redteam.md, AUDIT-2026-09-12-S16-census-peephole-followups.md, AUDIT-2026-09-12-S17-redteam.md -->

S15: reload-reuse precision (width underestimation is the only
miscompile route: `max(suffix, vector width 32/64)`; over-broad on
purpose), symmetric web-wide flag, confounder isolation in the gate.
S16: two more upstream peephole residuals with structural root causes;
PR #503 red-team done — the carry-table and implicit-operand classifier
are what their names say, the rename-window rollback is the escape
hatch; census-zero achieved and pinned (`census_full_delta.sh`, 5 opt
levels × 807 TUs). S17: the 0.3491 % −1,236-insn i686 store-forward
series verified (two independent kill switches per half — what made the
store side attributable at all); the "i686 extension is a 2-line change"
claim in a neighbouring doc **falsified** (`lccc-i686` does not use
`peephole/passes/`; its `i686/codegen/peephole.rs` is separate —
queued into the i686 subsystem doc). [audit][fix]

## 09-07–09-12 — `ms178-1` deliverable doc (rolling session summary)
<!-- src: engineering/ms178-1-session-deliverable.md -->

The rolling per-snapshot summary that accompanied the patch series
(rebase ledger S01→S17 across bases `b4ba9c5`…`0e4cf540`, the S16
census follow-ups, the #503 carry/implicit-operand audit, the i686
jump-table latent gap). All of its durable content is either in the
entries above, in [`../DECISIONS.md`](../DECISIONS.md), or in the
merged PRs it indexes. Nothing to re-derive. [rec]

## 09-10–09-12 — first i686 audit wave (addressing gold + Astra RA kernel)
<!-- src: FOLLOWUP-2026-09-10-i686-addressing-gold.md, FOLLOWUP-2026-09-10-i686-f128-globals-magic-audit.md, FOLLOWUP-2026-09-10-i686-prologue-fastcall-regparm-audit.md, FOLLOWUP-2026-09-10-param-alignment-and-f128-u64-rail.md, FOLLOWUP-2026-09-10-regalloc-astra-kernel.md, FOLLOWUP-2026-09-11-regalloc-v3-redteam.md -->

Root-directory series, now consolidated into [`../subsystems/i686.md`](../subsystems/i686.md).
(1) **Addressing encoder GAS-parity rewrite** (`assembler/encoder/core.rs`):
validation-before-mutation, allocation-free address planning, every
semantic verified against GAS 2.47, differential corpus + CI gate
(merged as PR #470). (2) **F128 / globals / magic-division** ring:
Astra report adjudicated against GCC 14.2 `-m32` ABI oracle; x87 stack
model (`emit_f128_load_to_x87`, cast/store/call-result paths) and the
address-fold gates re-derived (PR #478 base 4721e8b). (3) **Prologue /
fastcall / regparm audit**: 12 findings reproduced-or-refuted against
GCC `-m32`; closes with the **param-alignment** fix (over-aligned
parameter allocas — Gap 1) and the x86-64 legacy `%rax` head in the
F128→U64 cast (Gap 2); i686 peephole `ret $N` misclassification fixed
on the way; merged casts work (PR #480) byte-identical verified. (4)
**Astra `regalloc.rs` kernel** (v1–v3): overlap kernel + flatten +
eligibility + seeds + FP fixes — Astra V1 correct on every P0
mechanism; two of its own proposals wrong (zero-length skip,
destructive-SIMD replacement) and withdrawn by V2; V3 batch agreed on
9/11 items with the two disagreements proven with IR + measurements
(phi-web exemption must stay — the hole *is* the proof; the
abort-on-extra-src-def refresh is sound and required for adler32-class
latches). `backend::regalloc` tests 54/54. [fix][audit]

## 09-11 — worst-10 triage lands: S05 inline-single-site, S06 cmp-fusion, S07 ifcombine
<!-- src: FOLLOWUP-2026-09-11-rebase-perf-s04-s07.md, FOLLOWUP-2026-09-11-boolpair-jmp-and-loop-memset.md, backlog.md (narrative folded here) -->

The triage line that fills `backlog.md`'s worst-12 table (lz4 1.941×,
with the true work gap ~10× once the ~2 ms fixed startup cost is
exposed by PASSES scaling — INF-HARNESS-1 in the live backlog): **S05** single-call-site statics stay outlined at
normal `-O2` (the spectral_norm/hash_table repair from W1, verified);
**S06** compare/branch fusion — the audit found **four latent holes**
(pure flag preservation, scheduling-window, ZEXT mask, `k_inplace_three`
guard) fixed with 16 targeted tests; **S07** ifcombine
reject-unless-branch-free (lz4 +4.7 % Ir). The next audit pass found the
**bool-pair fused arm was the only branch emitter without the tail
`jmp`** (VALID finding, worse than the report phrased it — fall-through
into the fused arm's condition inversion) — fixed, plus the loop-memset
idiom landing (lz4 −1.75 % Ir). Rebase numbering: pre-rebase working
names (S04-inline…) map onto ledger numbers S05/S06/S07 (ledger S04 =
the oracle-label fix; ledger S08 = the boolpair+loop-memset idea). The
root `backlog.md`'s *narrative* of this saga is folded here; the live
item tables stay in `backlog.md`. [goal][fix]

## 09-12 — i686 CFG slot-forwarding + narrow-store miscompile (S19/PR #512)
<!-- src: FOLLOWUP-2026-09-12-i686-cfg-slot-forwarding-and-narrow-store-miscompile.md (half merged as PR #512), FOLLOWUP-2026-09-12-i686-x87-gp-pair-staging.md -->

PR #512 (`bcfeeefe`) carries: CFG-aware frame-slot forwarding, the
**narrow-store miscompile fix**, the `godbolt.py` icc-label fix and the
S17 red-team audit; remainder on branch s19: constant call arguments
stored **directly** instead of routed through `%eax`, one shared
constant-materialization model for both marshalling paths (6 unit tests
+ end-to-end regression), the oracle-methodology correction + the
constant-fold decision record. The x87 GP-pair document states the
week's headline honestly: **the dominant cause of the i686 runtime gap
vs GCC is x87↔GP-pair staging**, measured; only the bounded load-side
fold is landed (§4.4/§4.5 of the original); the root fix is **not**
done and its contract lives in [`../subsystems/i686.md`](../subsystems/i686.md).
[fix][goal]

## 09-05..09-12 — docs mopping (CPU-model, APX/EVEX, FP-minmax)
<!-- src: docs/FOLLOWUP_CPU_MODEL.md, docs/CPU_MODEL_AUDIT.md, docs/FOLLOWUP_ASSEMBLER_APX.md, docs/FOLLOWUP_ASSEMBLER_EVEX.md, docs/FOLLOWUP_CODEGEN_APX.md -->

Docs-pair consolidation: CPU tuning model (PR #397/#399 audit + v3
wiring) → [`../subsystems/cpu-model.md`](../subsystems/cpu-model.md)
(RPL row provenance by construction = ADL-P uops; `Generic` envelope =
historical constants; vzeroupper arming + string-op liveness pierced
the peephole — `is_string_instruction` prefix-aware; rep_movsb scaling
2048/8192/16384/2112). APX/EVEX (assembler + ISel contract) →
[`../subsystems/x86-apx-evex.md`](../subsystems/x86-apx-evex.md) (off by
default; `-mapx`/`-mapxf` explicit only; `-march=raptorlake` must NEVER
emit APX/EGPR — #UD on the 14700KF; thread-local gate
`isel::set_apx_enabled`; GAS **2.47** pinned oracle at
`~/.cache/gas-2.47-x86_64/bin/as`; size wins kept: REX2 over EVEX,
VEX over EVEX, `movl` zero-ext over `movabs`). FP-minmax in
[`2026-09-W1.md`](2026-09-W1.md#w1-close-out). [rec]

---

## Deleted-source map (45 documents → entries above)

`engineering/FOLLOWUP-2026-09-06-{RUST-MODERNIZATION-AND-WORKLOAD-CORPUS,
kernel-boot-c1-c5-prologue-memcpy,machinst-window-allocator,ms08-ra-config,
pf15-widened-byte-carriers,pf17-header-effects-narrow-cmp,pr425-ci-green,
pr425-perf41-audit,rust2024-ci-hygiene}.md`,
`engineering/FOLLOWUP-2026-09-06B-REDTEAM-AUDIT-PATCH-VERDICT.md` ·
`engineering/FOLLOWUP-2026-09-07-{ARX-CHACHA-RA,kernel-boot-c6-os-agg-frame,
loop-rotate-guard-g,rotate-imm32-gaddr-then-slot-traffic,
v5-rdx-hazard-pf07-riscv-imm12}.md` ·
`engineering/FOLLOWUP-2026-09-08-{OPOS05CD-BYTE-LANE-MAPS,UNIFIED-442-445,
loop-alignment-identical-blocks}.md` (09-08-OP05CD spell per ls) ·
`engineering/AUDIT-2026-09-08-{PR444-redteam,S03-supreme-merge}.md` ·
`engineering/FOLLOWUP-2026-09-09-memcpy-lowering-and-fp-soundness.md`,
`FOLLOWUP-2026-09-09-cross-backend-oracle-and-four-miscompiles.md` (root) ·
`engineering/FOLLOWUP-2026-09-11-{global-location-allocation-phase1,
phi-acyclic-copy-order,valve-cost-blindness,x86-slot-load-dedup}.md` ·
`engineering/AUDIT-2026-09-11-S12-redteam.md`,
`engineering/AUDIT-2026-09-12-{S15-redteam,S16-census-peephole-followups,
S17-redteam}.md` ·
root `FOLLOWUP-2026-09-10-{i686-addressing-gold,i686-f128-globals-magic-audit,
i686-prologue-fastcall-regparm-audit,param-alignment-and-f128-u64-rail,
regalloc-astra-kernel}.md`, root `FOLLOWUP-2026-09-11-{regalloc-v3-redteam,
rebase-perf-s04-s07,boolpair-jmp-and-loop-memset}.md`,
root `FOLLOWUP-2026-09-12-{i686-cfg-slot-forwarding-and-narrow-store-miscompile,
i686-x87-gp-pair-staging}.md` (all digested into the 09-10–09-12 entries;
i686 durables consolidated in `subsystems/i686.md`) ·
`engineering/ms178-1-session-deliverable.md`,
`artifacts/repros/rot16-arx-O2-miscompile.md` (the `.c` repros and
`killswitch_bisect.sh`/`delta_shrink.py` in that dir are kept) ·
`docs/{CPU_MODEL_AUDIT,FOLLOWUP_CPU_MODEL,FOLLOWUP_ASSEMBLER_APX,
FOLLOWUP_ASSEMBLER_EVEX,FOLLOWUP_CODEGEN_APX}.md` (durable content moved
to subsystems as §above).
