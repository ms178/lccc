# lccc-only: exercises lccc's linker resolving the lowercase @gottpoff TLS IE
# relocation. GCC + GNU ld resolve it incorrectly in non-PIE executables
# (second read returns a stale value), so the GCC reference is not a valid
# oracle; lccc's self-check (42 then 7) passes.
LCCC_NO_COMPARE=1
