CCC_LOOP_ROTATE=1
# Exercise rotation of a second sequential counted loop (PF-17 pred-label).
# Vectorize would rewrite both counted loops into Vec* bodies, which
# rotation conservatively bails on (Intrinsic). Keep them scalar so the
# guard-label incoming is actually constructed.
CCC_DISABLE_PASSES=vectorize
