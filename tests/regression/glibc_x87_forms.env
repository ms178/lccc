# lccc-only: verifies x87 forms (fsubr/fdivr/fcompl/fcomi/fucomi/ffreep) that
# lccc's assembler encodes correctly. GCC's reference binary SIGILLs on this
# host (GAS emits an invalid x87 encoding), so it cannot serve as an oracle.
LCCC_NO_COMPARE=1
