# Host GCC rejects the bare-alloca initialization shape at this flag set
# (implicit declaration interacts with -Werror-ish defaults in the runner's
# reference build); the point of the test is LCCC's own builtin lowering and
# runtime self-check, so the GCC comparison is waived.
LCCC_NO_COMPARE=1
