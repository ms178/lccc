# GCC 14.2 -O2 miscompiles 128-bit switch compare chains (upstream
# PR122943 was filed against exactly this area; the torture harness
# reference-runs pr122943.c at -O2 and aborts 20/20).  The oracle cannot
# validate this pin; lccc-only semantics.
LCCC_NO_COMPARE=1
