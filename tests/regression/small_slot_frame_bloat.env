# The 8-byte-slot configuration (CCC_NO_SMALL_SLOTS=1) cannot survive
# recurse(850) within the default stack limit — that overflow IS the bug
# this test pins. Value correctness is covered by the GCC oracle compare.
LCCC_NO_AB=1
