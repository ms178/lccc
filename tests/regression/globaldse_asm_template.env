# F5: GCC at -O2 deletes the static and fails to link when the only reader
# is an inline-asm template string (same gap as lccc had before fix).
# LCCC must keep the store; this test is lccc-only, not compared vs GCC.
LCCC_NO_COMPARE=1
