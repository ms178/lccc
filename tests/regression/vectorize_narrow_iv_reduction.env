# Exercise the rotated narrow-I8/I16 comparison path as well as the
# vectorizer's scalar fallback. The compiler must emit assembly GAS can
# assemble, then link and run bit-exactly against GCC.
CCC_LOOP_ROTATE=1
