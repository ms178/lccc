# Historical micro-benchmark harness (archive)

**Canonical runner:** `tests/benchmark/run_benchmarks.py`  
**Canonical policy:** `docs/benchmarks.md`

This directory (`bench.py`, `01_arith_loop.c`, …, `BENCHMARK_RESULTS*.md`) is
the **March 2026** five-kernel suite. Do not cite those times as current.
Do not use `/home/min/lccc` or `ccc` binary names.

SQLite “2h06m vs GCC” numbers describe **pre-RA CCC**, not LCCC 2026-08.

For RA evidence on gzip / zlib-ng / Expat see
`../register-allocation/VALIDATION_ZLIB_GZIP_EXPAT.md`.
